<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="topics.mybatis.MyBatisStudentMapper">
    <resultMap id="studentSummaryMap" type="MyBatisStudentSummary">
        <id column="student_id" property="id"/>
        <result column="name" property="studentName"/>
        <result column="grade" property="className"/>
        <result column="score" property="score"/>
    </resultMap>

    <select id="findAll" resultType="MyBatisStudent">
        SELECT student_id, name, grade, score FROM mybatis_student ORDER BY student_id
    </select>

    <select id="findById" resultType="MyBatisStudent">
        SELECT student_id, name, grade, score
        FROM mybatis_student
        WHERE student_id = #{studentId}
    </select>

    <insert id="insert" parameterType="MyBatisStudent" useGeneratedKeys="true" keyProperty="studentId">
        INSERT INTO mybatis_student (name, grade, score)
        VALUES (#{name}, #{grade}, #{score})
    </insert>

    <update id="update" parameterType="MyBatisStudent">
        UPDATE mybatis_student
        SET name = #{name}, grade = #{grade}, score = #{score}
        WHERE student_id = #{studentId}
    </update>

    <delete id="deleteById">
        DELETE FROM mybatis_student
        WHERE student_id = #{studentId}
    </delete>

    <select id="findAllWithResultMap" resultMap="studentSummaryMap">
        SELECT student_id, name, grade, score FROM mybatis_student ORDER BY student_id
    </select>
</mapper>
